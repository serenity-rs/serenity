[package]
name = "13_gateway_intents"
version = "0.1.0"
authors = ["my name <my@email.address>"]
edition = "2018"

[dependencies]
serenity = { path = "../../", default-features = false, features = ["client", "rustls_backend"] }
tokio = { version = "0.2", features = ["macros"] }
